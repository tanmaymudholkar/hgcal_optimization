#!/bin/bash

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/HGcal__version44_model2_BOFF_runno_${j}_et100_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/HGcal__version44_model2_BOFF_runno_${j}_et3_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/HGcal__version44_model2_BOFF_runno_${j}_et20_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/HGcal__version44_model2_BOFF_runno_${j}_et50_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

# j=""
# if [ ${#j} -ge 1 ]; then
#     echo "$j"
#     echo "nonempty"
# fi




for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/HGcal__version34_model2_BOFF_runno_${j}_et100_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/HGcal__version34_model2_BOFF_runno_${j}_et3_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/HGcal__version34_model2_BOFF_runno_${j}_et20_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/HGcal__version34_model2_BOFF_runno_${j}_et50_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done






























for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/Digi__version44_model2_BOFF_runno_${j}_et100_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/Digi__version44_model2_BOFF_runno_${j}_et3_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/Digi__version44_model2_BOFF_runno_${j}_et20_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_vflat/Digi__version44_model2_BOFF_runno_${j}_et50_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_vflat/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

# j=""
# if [ ${#j} -ge 1 ]; then
#     echo "$j"
#     echo "nonempty"
# fi




for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/Digi__version34_model2_BOFF_runno_${j}_et100_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/Digi__version34_model2_BOFF_runno_${j}_et3_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/Digi__version34_model2_BOFF_runno_${j}_et20_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done

for j in {1..100}; do
    # echo "${j}"
    filename=$(ls export_dir/high_stats_version_34/Digi__version34_model2_BOFF_runno_${j}_et50_eta2.100.root)
    doesnotexist=$(echo "$filename" | grep "not")
    # if [-a export_dir/high_stats_version_34/*runno_${j}_*]
    # then
    # 	echo "some file exists"
    # fi

    # echo "$doesnotexist"

    if [ ${#doesnotexist} -ge 1 ]; then
	echo $filename,"does not exist"
    fi
    
done
